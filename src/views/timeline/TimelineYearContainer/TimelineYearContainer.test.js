it('should show a Timeline Year View with action bar', () => {
});

it('should call #handleTodoChange if any todo changed', () => {
});

it('should refresh todos if todosUrl or any todo changed', () => {
});

it('should call #handleMonthChange if start or end month changed', () => {
});

it('should call #handleTodayClick if today button clicked', () => {
});

it('should call #handlePreviousYearClick if previous year button clicked', () => {
});

it('should call #handleNextYearClick if next year button clicked', () => {
});